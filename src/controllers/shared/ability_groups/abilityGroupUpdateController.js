import { response } from "express";

export const abilityGroupUpdateController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - abilityGroupUpdateController'
    });
}