import { response } from "express";

export const abilityGroupDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - abilityGroupDeleteController'
    });
}