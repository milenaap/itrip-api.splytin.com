import { response } from "express";

export const abilityGroupStoreController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - abilityGroupStoreController'
    });
}