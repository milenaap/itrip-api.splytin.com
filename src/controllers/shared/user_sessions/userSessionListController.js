import { response } from "express";

export const userSessionListController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - userSessionListController'
    });
}