import { response } from "express";

export const userSessionStoreController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - userSessionStoreController'
    });
}