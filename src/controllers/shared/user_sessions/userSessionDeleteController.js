import { response } from "express";

export const userSessionDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - userSessionDeleteController'
    });
}