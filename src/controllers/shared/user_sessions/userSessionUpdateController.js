import { response } from "express";

export const userSessionUpdateController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - userSessionUpdateController'
    });
}