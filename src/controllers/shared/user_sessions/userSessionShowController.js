import { response } from "express";

export const userSessionShowController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - userSessionShowController'
    });
}