import { response } from "express";

export const abilityUsersListController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - abilityUsersListController'
    });
}