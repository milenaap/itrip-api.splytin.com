import { response } from "express";

export const abilityUserListController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - abilityUsersListController'
    });
}