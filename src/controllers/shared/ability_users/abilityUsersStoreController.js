import { response } from "express";

export const abilityUsersStoreController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - abilityUsersStoreController'
    });
}