import { response } from "express";

export const userStatusUpdateController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - userStatusUpdateController'
    });
}