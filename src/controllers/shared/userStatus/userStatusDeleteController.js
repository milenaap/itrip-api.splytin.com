import { response } from "express";

export const userStatusDeleteController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - userStatusDeleteController'
    });
}