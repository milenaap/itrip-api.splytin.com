import { response } from "express";

export const userStatusStoreController = async(req, res = response) => {

    //TODO

    return res.json({
        msg: 'API - userStatusStoreController'
    });
}